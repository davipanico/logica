var rob = [1, 2, 4, 5]

function calcularMedia(array) {
    var tamanhoDaLista = array.length;
    array.map( numero => {
        var soma 
    })
    return (soma / tamanhoDaLista)
}

var it = calcularMedia(rob)

console.log(it)